# RAG from scratch: retrieval quality experiments

Building a retrieval-augmented generation pipeline without frameworks or a
vector database, then measuring retrieval quality across configurations.

Local stack: sentence-transformers (bge-small-en-v1.5) for embeddings,
Ollama (llama3.1:8b) for generation. No external API calls.

**Status: in progress.**